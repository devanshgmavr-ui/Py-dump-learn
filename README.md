# Py-dump-learn
Python Lab Programs

A collection of beginner-to-intermediate Python programs covering core programming concepts — from basic I/O to file handling, matrices, and number theory.


📂 Programs
#FileDescription01HELLO_WORLD.pyThe classic first program — prints "Hello World" to the console. A clean, commented starting point for any Python journey.02Arithmetic_Operations.pyTakes two integers as input and displays their sum, difference, product, quotient, and average using formatted f-strings.03Largest_of_Three_Numbers.pyReads three integers and determines the largest using Python's built-in max() function. Concise and beginner-friendly.04Celsius_to_Fahrenheit.pyConverts a temperature from Celsius to Fahrenheit using the standard formula, displaying the result rounded to two decimal places.05Fahrenheit_to_Celsius.pyConverts a temperature from Fahrenheit to Celsius using the inverse formula, displaying the result rounded to two decimal places.06Leap_Year_Check.pyChecks whether a given year is a leap year using divisibility rules for 400, 100, and 4 — handles all edge cases correctly.07Factorial_Using_Loop.pyComputes the factorial of a given non-negative integer using an iterative loop approach, avoiding recursion overhead.08Fibonacci_Series_Using_Loop.pyGenerates and prints the Fibonacci series up to N terms using a loop with efficient tuple-swap variable updates.09Prime_Number_Check.pyDetermines if a given number is prime by trial division up to its square root — handles edge cases like numbers less than 2.10Reverse_a_Given_Integer.pyReverses the digits of a given integer, correctly preserving the sign of negative numbers using string slicing.11Palindrome_Check__String_.pyChecks if a string is a palindrome by comparing it with its reverse using Python's slice notation [::-1].12Palindrome_Check_Using_String_Library.pyAn enhanced palindrome checker that strips spaces and normalizes case before comparison — handles phrases like "A man a plan".13Count_Vowels_and_Words_in_a_String.pyCounts the total number of vowels and words in a user-input string using sum() with a generator and str.split().14Grade_Calculator.pyCalculates total marks and percentage across 5 subjects, then assigns a letter grade (A–F) based on percentage thresholds.15Array_Operations__max__min__sum__avg_.pyReads N integers into a list and displays the maximum, minimum, sum, and average using Python's built-in functions.16Roots_of_a_Quadratic_Equation_pySolves a quadratic equation using the discriminant — handles two real roots, one repeated root, and complex roots via cmath.17Matrix_Addition__Subtraction__Multiplication.pyPerforms addition, subtraction, and multiplication on two user-defined matrices; multiplication is skipped for non-square inputs.18Menu-Driven_Calculator__Switch-Case_.pyA menu-driven calculator using Python's match-case statement supporting addition, subtraction, multiplication, and safe division.19File_Create_and_Write_Operations.pyCreates a text file, writes student data (name, roll, subject, department), and reads it back — demonstrates basic file I/O.20Copy_Content_from_One_File_to_Another.pyCopies content from student_data.txt to a new file, then prints the copied content — includes error handling for missing source.
